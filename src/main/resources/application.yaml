spring:
  application:
    name: SpringAiDemo2
  ai:
    openai:
      api-key: ${OPENAI_API_KEY} # 从环境变量当中读取密钥
      base-url: https://coding.dashscope.aliyuncs.com/v1
      chat:
        model: qwen3.7-plus
    ollama:
      base-url: http://192.168.31.156:11434
      chat:
        model: qwen3.5:4b
logging:
  level:
    org.springframework.ai: DEBUG # 若要使得配置的内置Advisor在控制台能打印日志，则需要将此级别设置为DEBUG